import aria from './aria.js';
import quinn from './quinn.js';
import rex from './rex.js';
import sage from './sage.js';

export const agents = { aria, quinn, rex, sage };
export const agentsList = [aria, quinn, rex, sage];
export default agents;
